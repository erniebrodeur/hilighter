package rules_test

import (
	. "github.com/onsi/ginkgo/v2"
)

var _ = Describe("Rules", func() {
	It("compiles PCRE-compatible patterns from YAML config", Pending)
	It("rejects invalid regex patterns with useful errors", Pending)
	It("ships built-in rule packs for common command output", Pending)
})
