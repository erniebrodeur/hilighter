package engine_test

import (
	. "github.com/onsi/ginkgo/v2"
)

var _ = Describe("Engine", func() {
	It("applies rules in declaration order when multiple patterns overlap", Pending)
	It("defaults rules to substring scope when no scope is set", Pending)
	It("supports whole-line scope without changing the source text content", Pending)
	It("maps capture groups to semantic labels for downstream rendering", Pending)
	It("processes input line by line", Pending)
})
