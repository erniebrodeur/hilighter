package theme_test

import (
	. "github.com/onsi/ginkgo/v2"
)

var _ = Describe("Theme loading", func() {
	It("loads semantic styles from YAML", Pending)
	It("ships a Monokai-style default theme", Pending)
	It("keeps the default theme foreground-only without background colors", Pending)
	It("resolves style labels used by rules and capture groups", Pending)
})
